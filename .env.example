# Copy this file to `.env` and fill in your values.
# `.env` is gitignored and never committed.

# Required: your OpenAI API key (used only when generating real samples).
OPENAI_API_KEY=your_openai_api_key_here

# Optional: model to use. Defaults to gpt-4.1-mini if unset.
MODEL=gpt-4.1-mini
